# Queue

### Запуск

Запуск с использованием конфигурационного файла, который лежит по пути ```./config``` 
#### Пример конфигурационного файла

```yaml
env: "local"
http_server:
    address: "localhost:8080"
    timeout: 4s
    idle_timeout: 30s
```
